{
    "tasks": {
        "dev": "deno run -A --unstable --local-npm npm:vite",
        "build": "deno run -A --unstable --local-npm npm:vite build",
        "preview": "deno run -A --unstable --local-npm npm:vite preview"
    }
}